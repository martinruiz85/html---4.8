/****** Object:  StoredProcedure [dbo].[spAlterViewsForCentralizePromosDb]    Script Date: 08/10/2025 03:51:40 p. m. ******/
DROP PROCEDURE IF EXISTS [dbo].[spAlterViewsForCentralizePromosDb]
GO
/****** Object:  StoredProcedure [dbo].[spAlterViewsForCentralizePromosDb]    Script Date: 08/10/2025 03:51:40 p. m. ******/
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE PROCEDURE spAlterViewsForCentralizePromosDb
 @dataBase NVARCHAR(510), @dataBaseMaster NVARCHAR(510),      
 @SubsidiaryId int, @LoggedUser VARCHAR(100)                         
 AS              
 BEGIN              

   DECLARE @returnTemp table (returnId int)      
   DECLARE @Date DateTime       
   DECLARE  @dynsql NVARCHAR(max),@CountSQLQuery varchar(30),@ParmDefinition nvarchar(500), @existsTable INT             
       
  SET @Date = GETDATE();           
  SET @ParmDefinition = N'@result varchar(30) OUTPUT';            
      
 SET  @dynsql =  N'USE ' + Quotename(@dataBaseMaster)             
 + N' IF  EXISTS (SELECT * FROM dbo.sysobjects WHERE id = OBJECT_ID(N''[dbo].[NP_Promocion]'') AND OBJECTPROPERTY(id, N''IsUserTable'')= 1)            
   SELECT @result = 1            
   ELSE             
   SELECT @result = 0'              
  EXEC Sp_executesql @dynsql,@ParmDefinition ,@result=@CountSQLQuery OUTPUT            
  SELECT @existsTable = CAST(@CountSQLQuery as int)            
  IF (@existsTable = 1)            
   BEGIN               
   SET @dynsql =  N'ALTER VIEW [dbo].[NP_Promocion]              
  AS              
  SELECT  *              
  FROM ' +   @dataBaseMaster+'.dbo.[NP_Promocion] go'              
  set @dynsql = 'exec ' + @dataBase + '..sp_executesql N''' + @dynsql + ''''              
   EXEC Sp_executesql @dynsql                

   INSERT INTO @returnTemp (returnId)      
    SELECT 15      
   END                 
        
            
 SET  @dynsql =  N'USE ' + Quotename(@dataBaseMaster)             
 + N' IF  EXISTS (SELECT * FROM dbo.sysobjects WHERE id = OBJECT_ID(N''[dbo].[NP_PromocionLog]'') AND OBJECTPROPERTY(id, N''IsUserTable'')= 1)            
   SELECT @result = 1            
   ELSE             
   SELECT @result = 0'              
  EXEC Sp_executesql @dynsql,@ParmDefinition ,@result=@CountSQLQuery OUTPUT            
  SELECT @existsTable = CAST(@CountSQLQuery as int)            
  IF (@existsTable = 1)            
   BEGIN               
   SET @dynsql =  N'ALTER VIEW [dbo].[NP_PromocionLog]              
  AS              
  SELECT  *              
  FROM ' +   @dataBaseMaster+'.dbo.[NP_PromocionLog] go'              
  set @dynsql = 'exec ' + @dataBase + '..sp_executesql N''' + @dynsql + ''''              
   EXEC Sp_executesql @dynsql                

   INSERT INTO @returnTemp (returnId)      
    SELECT 37      
   END                     
               
 SET  @dynsql =  N'USE ' + Quotename(@dataBaseMaster)             
 + N' IF  EXISTS (SELECT * FROM dbo.sysobjects WHERE id = OBJECT_ID(N''[dbo].[NP_ConfiguracionPromocion]'') AND OBJECTPROPERTY(id, N''IsUserTable'')= 1)            
   SELECT @result = 1            
   ELSE             
   SELECT @result = 0'              
  EXEC Sp_executesql @dynsql,@ParmDefinition ,@result=@CountSQLQuery OUTPUT            
  SELECT @existsTable = CAST(@CountSQLQuery as int)            
  IF (@existsTable = 1)            
   BEGIN               
   SET @dynsql =  N'ALTER VIEW [dbo].[NP_ConfiguracionPromocion]              
  AS              
  SELECT  *              
  FROM ' +   @dataBaseMaster+'.dbo.[NP_ConfiguracionPromocion] go'              
  set @dynsql = 'exec ' + @dataBase + '..sp_executesql N''' + @dynsql + ''''              
   EXEC Sp_executesql @dynsql                
     
   INSERT INTO @returnTemp (returnId)      
    SELECT 14      
   END        
            
 SET  @dynsql =  N'USE ' + Quotename(@dataBaseMaster)             
 + N' IF  EXISTS (SELECT * FROM dbo.sysobjects WHERE id = OBJECT_ID(N''[dbo].[NP_ConfiguracionPromocionLog]'') AND OBJECTPROPERTY(id, N''IsUserTable'')= 1)            
   SELECT @result = 1            
   ELSE             
   SELECT @result = 0'              
  EXEC Sp_executesql @dynsql,@ParmDefinition ,@result=@CountSQLQuery OUTPUT            
  SELECT @existsTable = CAST(@CountSQLQuery as int)            
  IF (@existsTable = 1)            
   BEGIN               
   SET @dynsql =  N'ALTER VIEW [dbo].[NP_ConfiguracionPromocionLog]              
  AS              
  SELECT  *              
  FROM ' +   @dataBaseMaster+'.dbo.[NP_ConfiguracionPromocionLog] go'              
  set @dynsql = 'exec ' + @dataBase + '..sp_executesql N''' + @dynsql + ''''              
   EXEC Sp_executesql @dynsql                

   INSERT INTO @returnTemp (returnId)      
    SELECT 38      
   END     
    
    
 SET  @dynsql =  N'USE ' + Quotename(@dataBaseMaster)             
 + N' IF  EXISTS (SELECT * FROM dbo.sysobjects WHERE id = OBJECT_ID(N''[dbo].[GrupoPersonalizado]'') AND OBJECTPROPERTY(id, N''IsUserTable'')= 1)            
   SELECT @result = 1            
   ELSE             
   SELECT @result = 0'              
  EXEC Sp_executesql @dynsql,@ParmDefinition ,@result=@CountSQLQuery OUTPUT            
  SELECT @existsTable = CAST(@CountSQLQuery as int)            
  IF (@existsTable = 1)            
   BEGIN               
   SET @dynsql =  N'ALTER VIEW [dbo].[GrupoPersonalizado]              
  AS              
  SELECT  *              
  FROM ' +   @dataBaseMaster+'.dbo.[GrupoPersonalizado] go'              
  set @dynsql = 'exec ' + @dataBase + '..sp_executesql N''' + @dynsql + ''''              
   EXEC Sp_executesql @dynsql                

   INSERT INTO @returnTemp (returnId)      
    SELECT 64      
   END        
            
 SET  @dynsql =  N'USE ' + Quotename(@dataBaseMaster)             
 + N' IF  EXISTS (SELECT * FROM dbo.sysobjects WHERE id = OBJECT_ID(N''[dbo].[GrupoPersonalizadoLog]'') AND OBJECTPROPERTY(id, N''IsUserTable'')= 1)            
   SELECT @result = 1            
   ELSE             
   SELECT @result = 0'              
  EXEC Sp_executesql @dynsql,@ParmDefinition ,@result=@CountSQLQuery OUTPUT            
  SELECT @existsTable = CAST(@CountSQLQuery as int)            
  IF (@existsTable = 1)            
   BEGIN               
   SET @dynsql =  N'ALTER VIEW [dbo].[GrupoPersonalizadoLog]              
  AS              
  SELECT  *              
  FROM ' +   @dataBaseMaster+'.dbo.[GrupoPersonalizadoLog] go'              
  set @dynsql = 'exec ' + @dataBase + '..sp_executesql N''' + @dynsql + ''''              
   EXEC Sp_executesql @dynsql                

   INSERT INTO @returnTemp (returnId)      
    SELECT 65      
   END        
    
    
    SET  @dynsql =  N'USE ' + Quotename(@dataBaseMaster)             
 + N' IF  EXISTS (SELECT * FROM dbo.sysobjects WHERE id = OBJECT_ID(N''[dbo].[DetalleGrupoPersonalizado]'') AND OBJECTPROPERTY(id, N''IsUserTable'')= 1)            
   SELECT @result = 1            
   ELSE             
   SELECT @result = 0'              
  EXEC Sp_executesql @dynsql,@ParmDefinition ,@result=@CountSQLQuery OUTPUT            
  SELECT @existsTable = CAST(@CountSQLQuery as int)            
  IF (@existsTable = 1)            
   BEGIN               
   SET @dynsql =  N'ALTER VIEW [dbo].[DetalleGrupoPersonalizado]              
  AS              
  SELECT  *              
  FROM ' +   @dataBaseMaster+'.dbo.[DetalleGrupoPersonalizado] go'              
  set @dynsql = 'exec ' + @dataBase + '..sp_executesql N''' + @dynsql + ''''              
   EXEC Sp_executesql @dynsql                

   INSERT INTO @returnTemp (returnId)      
    SELECT 66      
   END        
            
 SET  @dynsql =  N'USE ' + Quotename(@dataBaseMaster)             
 + N' IF  EXISTS (SELECT * FROM dbo.sysobjects WHERE id = OBJECT_ID(N''[dbo].[DetalleGrupoPersonalizadoLog]'') AND OBJECTPROPERTY(id, N''IsUserTable'')= 1)            
   SELECT @result = 1            
   ELSE             
   SELECT @result = 0'              
  EXEC Sp_executesql @dynsql,@ParmDefinition ,@result=@CountSQLQuery OUTPUT            
  SELECT @existsTable = CAST(@CountSQLQuery as int)            
  IF (@existsTable = 1)            
   BEGIN               
   SET @dynsql =  N'ALTER VIEW [dbo].[DetalleGrupoPersonalizadoLog]              
  AS              
  SELECT  *              
  FROM ' +   @dataBaseMaster+'.dbo.[DetalleGrupoPersonalizadoLog] go'              
  set @dynsql = 'exec ' + @dataBase + '..sp_executesql N''' + @dynsql + ''''              
   EXEC Sp_executesql @dynsql                

   INSERT INTO @returnTemp (returnId)      
    SELECT 67      
   END    
    
    
       SET  @dynsql =  N'USE ' + Quotename(@dataBaseMaster)             
 + N' IF  EXISTS (SELECT * FROM dbo.sysobjects WHERE id = OBJECT_ID(N''[dbo].[NP_ExcepcionesDePlatillos]'') AND OBJECTPROPERTY(id, N''IsUserTable'')= 1)            
   SELECT @result = 1            
   ELSE             
   SELECT @result = 0'              
  EXEC Sp_executesql @dynsql,@ParmDefinition ,@result=@CountSQLQuery OUTPUT            
  SELECT @existsTable = CAST(@CountSQLQuery as int)            
  IF (@existsTable = 1)            
   BEGIN               
   SET @dynsql =  N'ALTER VIEW [dbo].[NP_ExcepcionesDePlatillos]              
  AS              
  SELECT  *              
  FROM ' +   @dataBaseMaster+'.dbo.[NP_ExcepcionesDePlatillos] go'              
  set @dynsql = 'exec ' + @dataBase + '..sp_executesql N''' + @dynsql + ''''              
   EXEC Sp_executesql @dynsql                

   INSERT INTO @returnTemp (returnId)      
    SELECT 68      
   END        
            
 SET  @dynsql =  N'USE ' + Quotename(@dataBaseMaster)             
 + N' IF  EXISTS (SELECT * FROM dbo.sysobjects WHERE id = OBJECT_ID(N''[dbo].[NP_ExcepcionesDePlatillosLog]'') AND OBJECTPROPERTY(id, N''IsUserTable'')= 1)            
   SELECT @result = 1            
   ELSE             
   SELECT @result = 0'              
  EXEC Sp_executesql @dynsql,@ParmDefinition ,@result=@CountSQLQuery OUTPUT            
  SELECT @existsTable = CAST(@CountSQLQuery as int)            
  IF (@existsTable = 1)            
   BEGIN               
   SET @dynsql =  N'ALTER VIEW [dbo].[NP_ExcepcionesDePlatillosLog]              
  AS              
  SELECT  *              
  FROM ' +   @dataBaseMaster+'.dbo.[NP_ExcepcionesDePlatillosLog] go'              
  set @dynsql = 'exec ' + @dataBase + '..sp_executesql N''' + @dynsql + ''''              
   EXEC Sp_executesql @dynsql                

   INSERT INTO @returnTemp (returnId)      
    SELECT 69      
   END    
   
   SELECT * from @returnTemp
   DECLARE @otherinfo VARCHAR(500) = CONCAT('Nueva bd maestra: ', @dataBaseMaster)
   --EXECUTE wansoftAdmin..spInsSupportWebLog @SubsidiaryId, 4,106, @Date, @LoggedUser, 0, @otherinfo
   select [identity] = @SubsidiaryId, 
		  [idTypeSupportWebLog] = 4,
  		  [idTypeMovementSupportWebLog] = 106, 
  		  [DateExecute] = @Date, 
  		  [user] = @LoggedUser


 END


GO
